require 'spec_helper'

describe ActiveReplicas do
  it 'has a version number' do
    expect(ActiveReplicas::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
